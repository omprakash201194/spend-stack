{
  "name": "@spendstack/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Core domain models, entities, and business rules for SpendStack.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"No tests yet for @spendstack/domain\"",
    "clean": "rm -rf dist"
  }
}
