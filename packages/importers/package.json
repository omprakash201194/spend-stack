{
  "name": "@spendstack/importers",
  "version": "0.1.0",
  "private": true,
  "description": "File importers and data ingestion adapters for SpendStack.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"No tests yet for @spendstack/importers\"",
    "clean": "rm -rf dist"
  }
}
