{
  "name": "@spendstack/database",
  "version": "0.1.0",
  "private": true,
  "description": "Database layer and data access utilities for SpendStack.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"No tests yet for @spendstack/database\"",
    "clean": "rm -rf dist"
  }
}
