{
  "name": "@spendstack/application",
  "version": "0.1.0",
  "private": true,
  "description": "Application use cases and orchestration layer for SpendStack.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"No tests yet for @spendstack/application\"",
    "clean": "rm -rf dist"
  }
}
